# TODO

## Theme: ensure light mode is default
- [x] Inspect theme logic in `src/App.jsx` and CSS variables in `src/index.css`.
- [x] Force default theme to light (do not follow system theme).
- [x] Add an early script in `index.html` to apply `html.dark` before React hydration (avoids FOUC if user previously saved dark).


